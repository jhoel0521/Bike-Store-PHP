# URL base pública (producción debe usar https)
BASE_URL=https://mi-dominio.com

# Conexión a la base de datos (en producción configura valores reales)
DB_HOST=db
DB_NAME=Bike_Store
DB_USER=bike_user
DB_PASSWORD=secure_change_me

# Contraseña root usada por scripts de mantenimiento (no la subas al repo)
DB_ROOT_PASSWORD=secure_root_change_me
